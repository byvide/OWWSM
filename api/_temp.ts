////////////////////////////////////////////////////////////////////////////////////////////////
type subroutineNumbers = [0, 1, 2, 3, 4, 5, 6, 7, 8];
type subroutineFormat<N extends number> = `Sub${N}`;
export type SubroutineREF = subroutineFormat<subroutineNumbers[number]>; //FIXME
////////////////////////////////////////////////////////////////////////////////////////////////
import { encodeHex } from "jsr:@std/encoding/hex";

// export const ENCODER = new TextEncoder(); //FIXME
export async function hashString(msg: string) {
    const msgBuffer = new TextEncoder().encode(msg);
    const hashBuffer = await crypto.subtle.digest("SHA-256", msgBuffer);
    return encodeHex(hashBuffer);
}
////////////////////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////////////////////
